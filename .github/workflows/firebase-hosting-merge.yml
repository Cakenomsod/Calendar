# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting + Functions on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}

    steps:
      - uses: actions/checkout@v4

      # 🔹 Deploy Hosting + Functions
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CALENDARPROJECT_F570E }}
          projectId: calendarproject-f570e
          deployTarget: "hosting,functions"  # <-- เพิ่มตรงนี้
          channelId: live  # ถ้าอยาก deploy แบบ preview channel สามารถเปลี่ยนเป็นชื่ออื่น
